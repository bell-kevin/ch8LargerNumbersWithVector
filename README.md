# ch8LargerNumbersWithVector
ask the user how many random numbers to generate. Declare a vector of unknown size, and use the push-back method to add the random numbers. Ask the user which number to test
